# Development overrides — extend docker-compose.yml
# Use: docker-compose up (automatically loads both files)

services:
  postgres:
    ports:
      - "5432:5432"

  identity:
    ports:
      - "${IDENTITY_PORT:-5101}:5101"

  training:
    ports:
      - "${TRAINING_PORT:-5201}:5201"

  corehr:
    ports:
      - "${COREHR_PORT:-5301}:5301"
