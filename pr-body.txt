Closes #37

## What

Employee directory page for CoreHR with full server-side pagination, sorting, search, and filtering.

## Changes

### Backend
- `TokenService`: Include tenant_id claim in JWT tokens for CoreHR access
- `CoreHRSeeder`: Seed 11 demo employees with manager hierarchy
- `Program.cs`: JsonStringEnumConverter for proper status serialization

### Frontend
- **@repo/ui**: Table, Select, Skeleton, Checkbox, DropdownMenu primitives
- **Employee directory** (`/core/employees`):
  - TanStack Table with server-side pagination & sorting
  - URL param state persistence (page, pageSize, sortBy, sortDir, search, filters)
  - Page size selector (10/25/50)
  - Breadcrumb navigation + Add Employee button
- **API integration**: Backend DTOs + mappers via @repo/api
- **Feedback components**: Reusable EmptyState, ErrorState
- **Sidebar**: Employees nav item

## Testing

1. Start backend: `docker-compose up -d` + Gateway
2. Login: admin@ey-hr.com / Admin@123456
3. Navigate to `/core/employees`
4. Verify:
   - Click column headers → Network shows sortBy/sortDir params
   - Change page size → URL updates, data refreshes
   - Search/filter → server-side filtering
   - Breadcrumb Home → navigates to `/`
   - Add Employee button visible (placeholder, no action)

## AC Checklist (#37)

- [x] Lists employees with real data
- [x] Search + filter + pagination (server-driven)
- [x] Server-side sorting with URL persistence
- [x] Uses @repo/api
- [x] Loading/error/empty states
