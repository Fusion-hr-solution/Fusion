## 1. Phase 1 — Canonical model and persistence

- [x] 1.1 Add `Employment` entity (employee, start/`EffectiveFrom`, optional end/`EffectiveTo`, status, employment type, source/provenance, audit) with `IEntityTypeConfiguration`, tenant-scoped indexes, and a one-active-employment-per-employee constraint strategy.
- [x] 1.2 Add `WorkAssignment` entity (employment ref, org unit, plain-text job title, optional work location, primary flag, effective from/to, source/provenance) with configuration, tenant indexes, and primary/overlap constraints.
- [x] 1.7 Implement and centralize the half-open `[EffectiveFrom, EffectiveTo)` interval convention (active-as-of predicate, close-and-succeed helper, overlap/contiguity checks) shared by `Employment`, `WorkAssignment`, and `ManagerRelationship`; add unit tests for boundary, adjacency, and null-end cases.
- [x] 1.3 Refactor `EmployeeReportingRelationship` into `ManagerRelationship` referencing `SubjectWorkAssignmentId`/`ManagerWorkAssignmentId`; update configuration FKs to `WorkAssignment`; keep effective dating and type. _(Added the refactored `ManagerRelationship` (WorkAssignment FKs) alongside the legacy `EmployeeReportingRelationship`, which still has Phase 2 consumers; the legacy entity is removed in the Phase 3 cleanup (task 10.1) to keep the build green at this phase boundary.)_
- [x] 1.4 Add append-only tenant-scoped `WorkforceAuditEntry` entity and configuration (tenant, entity type/id, action, actor, timestamp, effective date, source/import reference, change details).
- [x] 1.5 Register new DbSets and fail-closed tenant query filters in `CoreHRDbContext`; confirm `ApplyConfigurationsFromAssembly` picks up the new configurations.
- [x] 1.6 Create the EF migration that adds the canonical tables (canonical model added alongside legacy columns, which remain temporarily as backfill sources only). _(Migration `20260626195348_AddWorkforceCanonicalModel` — additive; creates Employments/WorkAssignments/ManagerRelationships/WorkforceAuditEntries with filtered-unique active/primary constraints.)_

## 2. Phase 1 — Backfill and fail-fast validation

- [x] 2.1 Implement deterministic, tenant-aware backfill: `Employment` from `HireDate`→`EffectiveFrom`/`Status`/`EmploymentType`, then primary `WorkAssignment` from org-context precedence/`JobTitle`/`WorkLocation`, then `ManagerRelationship` from `ManagerId`, all with `Source = Migration` provenance and `[from, to)` dating. _(`Infrastructure/Backfill/WorkforceBackfillService` — per-tenant, idempotent; inactive employees end at `UpdatedAt`.)_
- [x] 2.2 Implement the deterministic organization-context precedence (1: existing canonical assignment data, 2: primary/home `EmployeeOrgMembership`, 3: legacy `Employee.OrgUnitId`) and fail fast on disagreement or multiple active primary memberships per employee/date. _(`OrganizationContextBackfillResolver`.)_
- [x] 2.3 Implement fail-fast backfill validation (invalid/missing references, missing employee numbers for matching, missing/inactive org units, cross-tenant links, overlapping `[from, to)` facts, self-management, manager cycles, conflicting org-context sources, multiple active primary memberships, invalid responsible-manager refs) that aborts with actionable diagnostics — no silent defaults, no quarantine-and-continue. _(Integrated into the backfill service; aborts via `WorkforceBackfillException` carrying all diagnostics. Employee-number matching is an import-side concern (Section 6), not backfill.)_
- [x] 2.4 Add migration/backfill tests over representative data covering both success and each fail-fast condition. _(`WorkforceBackfillServiceTests` — 13 tests: success, idempotency, inactive end-dating, no-org gap, + each fail-fast code.)_

## 3. Phase 2 — Canonical resolvers and domain services

- [x] 3.1 Implement canonical resolver services (current employment, primary work assignment org/job/location, primary manager and manager chain, org-scope membership from `WorkAssignment.OrgUnitId`) with explicit `asOfDate` support. _(`Features/Workforce/Services/WorkforceCanonicalResolver` — `IWorkforceCanonicalResolver`, half-open as-of predicate, cycle-guarded chain. Consumers (readiness/authorization/contracts) are repointed in tasks 3.2–7.x.)_
- [x] 3.2 Implement explicit domain commands/services: `UpdateEmployeeProfile`, employment start/end, `ChangeWorkAssignment`, `ChangeManager`, with effective-dated history and per-action `WorkforceAuditEntry` writes in the same transaction. _(`Features/Workforce/Services/WorkforceMutationService` (`IWorkforceMutationService`) — unit-of-work staging (no internal `SaveChanges`, so the orchestrating handler commits fact + audit atomically in one transaction); close-and-succeed `[from, to)` transitions; canonical invariants (single active employment, single active primary assignment, org-unit tenant/active validity, manager self/cross-tenant/cycle); `Result<T>` with `Error` codes; Local-aware lookups so freshly-staged facts in a composed create resolve. Added focused `Employee.UpdateProfile` (identity-only). `WorkforceMutationServiceTests` — 17 tests across all four operations incl. audit writes, close-and-succeed, and each fail-fast branch. Consumed by endpoints/lifecycle/import in tasks 4.x–6.x.)_
- [x] 3.3 Add `OrgUnit.ResponsibleManagerEmployeeId` validation (same-tenant, active employment) to create/update org-unit handlers and audit changes; expose resolved responsible-manager data where UI/Performance needs it; enforce server-side deny-by-default authorization and tenant scoping on responsible-manager changes with authorization tests. _(`Features/OrgUnits/Services/ResponsibleManagerService` (`IResponsibleManagerService`) — validates same-tenant (fail-closed filter) employee with an active canonical `Employment` (via resolver, never legacy status); stages a `ResponsibleManagerChanged` audit only when the value changes, in the handler's transaction. Wired into Create/Update org-unit handlers; id already exposed on `OrgUnitDto`. Authz: controller is already deny-by-default (`[Authorize(Roles=HRAdmin)]` + `CanManageStructure`→Forbid); added method-level role assertions. `ResponsibleManagerServiceTests` (7: null/active/unknown/no-employment/cross-tenant/audit-changed/audit-unchanged) + controller authz Theory.)_
- [x] 3.4 If `WorkforceAuditEntry` is exposed through any read API, enforce server-side deny-by-default authorization and tenant scoping on that surface, with authorization and tenant-isolation tests. _(No read API for `WorkforceAuditEntry` exists in this milestone (writes only) — building an authorized read endpoint now would be dead code (D3/non-goals), so the conditional authz requirement is deferred to whenever a surface is introduced. The entity is already fail-closed tenant-scoped in `CoreHRDbContext`; added `WorkforceAuditTenantIsolationTests` (2) pinning that any future read returns only the current tenant's entries and nothing under an unresolved tenant.)_

## 4. Phase 2 — Employee endpoints and Employee Details

- [x] 4.1 Refactor `POST api/corehr/employees` to a composed create that orchestrates `Employee`, `Employment`, primary `WorkAssignment`, and optional `ManagerRelationship` atomically and returns `EmployeeDetails`. _(`CreateEmployeeCommandHandler` now stages `Employee` + `StartEmployment` + `ChangeWorkAssignment` + optional `ChangeManager` through `IWorkforceMutationService` and commits once; `POST /api/corehr/employees` returns the new Core-only `EmployeeDetailsDto` built by `EmployeeDetailsReadModelService` (canonical employment/assignment/manager, history summary, readiness, no Identity fields). Added `CreateEmployeeComposedCommandHandlerTests`; updated legacy handler tests to canonical semantics. Verified with `dotnet build Backend/EY.HRPlatform.CoreHR` and full `Backend/EY.HRPlatform.CoreHR.Tests` (495/495).)_
- [x] 4.2 Refactor `PUT api/corehr/employees/{id}` into a composed, non-generic update delegating to explicit domain services; reject lifecycle operations that require dedicated actions. _(`UpdateEmployeeCommandHandler` now returns `EmployeeDetailsDto`, uses `IWorkforceMutationService` for profile, active-employment, and active-primary-assignment corrections, and rejects generic `manager`/`hireDate` updates with dedicated-action validation errors. Added correction methods to `WorkforceMutationService`, `WorkAssignment.UpdateDetails`, `Employee.UpdateEmployeeNumber`, and `WorkforceAuditAction.EmploymentUpdated`; rewired `PUT /api/corehr/employees/{id}` to return the composed details response and map mutation failures. Added `UpdateEmployeeComposedCommandHandlerTests` and replaced legacy update-handler coverage with canonical expectations. Verified with `dotnet build Backend/EY.HRPlatform.CoreHR` and full `Backend/EY.HRPlatform.CoreHR.Tests` (489/489).)_
- [x] 4.3 Build the composed `EmployeeDetails` read model (Core-owned facts only) and update profile/list/by-key read endpoints to source employment/org/job/manager from canonical resolvers; move direct-report authorization scope to `ManagerRelationship`. _(`GetEmployeeById`/`GetEmployeeByKey` now return `EmployeeDetailsDto` via the canonical `EmployeeDetailsReadModelService`; profile/list/reporting-line queries now resolve employment/org/job/manager/direct reports from canonical facts first, with a transitional legacy-only fallback only when a fixture or pre-backfill row has no canonical facts yet. Direct-report scope now follows the returned manager fact sourced from `ManagerRelationship`, and reporting lines traverse canonical relationships with the same staged fallback. Added canonical-divergence coverage in employee read-handler tests. Verified with `dotnet build Backend/EY.HRPlatform.CoreHR` and full `Backend/EY.HRPlatform.CoreHR.Tests` (492/492).)_
- [x] 4.4 Re-implement tenant-publish org-unit retirement safety checks against active primary `WorkAssignment.OrgUnitId` instead of `Employee.OrgUnitId`. _(`PublishTenantStructureCommandHandler` now blocks retirement using active primary canonical `WorkAssignment` rows joined to active canonical `Employment` rows, with a narrow staged fallback to legacy `Employee.OrgUnitId` only for active employees that do not yet have an active primary assignment. Added publish-block regression coverage for canonical-only org assignment in `TenantSetupPublishingCommandHandlerTests`. Verified with `dotnet build Backend/EY.HRPlatform.CoreHR` and full `Backend/EY.HRPlatform.CoreHR.Tests` (493/493).)_

## 5. Phase 2 — Lifecycle actions

- [x] 5.1 Implement `POST {id}/terminate` (end active employment, primary assignment, and **subject-side** manager relationships at effective date; preserve employee/history) with active-employment validation and audit; do not auto-close manager-side relationships. _(`TerminateEmployeeCommandHandler` + `POST /api/corehr/employees/{id}/terminate` wired through new `IWorkforceMutationService.TerminateEmployeeAsync`; terminates canonical employment + primary assignment + subject-side manager links in one `SaveChanges`, preserves manager-side links for later 5.2 blocking, and writes transactional termination audit with optional note. Added service/handler/auth-route coverage; verified with targeted lifecycle tests, `dotnet build Backend/EY.HRPlatform.CoreHR --no-restore`, and full `Backend/EY.HRPlatform.CoreHR.Tests`.)_
- [x] 5.2 Implement termination direct-report blocking using active primary `ManagerRelationship` (incoming/manager-side links must already be ended via reassignment effective ≤ termination date), returning affected direct reports; no auto/skip-level/fallback reassignment. _(`WorkforceMutationService.TerminateEmployeeAsync` now blocks on active incoming primary `ManagerRelationship` rows pointing to the terminating assignment as of the termination date, using unit-of-work-aware local+persistent resolution so same-transaction reassignments effective ≤ termination date are honored. Blocking failures return `Employment.TerminationBlockedByDirectReports` with the affected direct reports identified in the validation message; no manager-side links are auto-closed or reassigned. Added mutation-service coverage for blocked termination, successful termination after same-date reassignment, and preserved pre-ended manager-side links. Verified with `dotnet build Backend/EY.HRPlatform.CoreHR --no-restore`, targeted lifecycle tests, and full `Backend/EY.HRPlatform.CoreHR.Tests`.)_
- [x] 5.3 Implement `POST {id}/rehire` (new employment + primary assignment + optional manager; never reopen prior employment) with validation and audit. _(`RehireEmployeeCommand`/`Handler` + `POST /api/corehr/employees/{id}/rehire` (`RehireEmployeeRequest`, If-Match concurrency) compose `StartEmployment` + `ChangeWorkAssignment` + optional `ChangeManager` through `IWorkforceMutationService` and commit once, returning `EmployeeDetailsDto`. `StartEmploymentAsync` rejects a second active employment, enforcing the no-active-employment precondition without reopening the prior (ended) employment; per-fact `WorkforceAuditEntry` rows are staged by the mutation service. `RehireEmployeeCommandHandlerTests` (2: rehire-after-termination creates new employment+assignment with prior ended unchanged; active-employment blocks with `Employment.AlreadyActive`).)_
- [x] 5.4 Implement `POST {id}/change-manager` (atomic end-current/create-new primary manager relationship using `[from, to)` close-and-succeed; self/cycle/cross-tenant/date validation; first-manager case) with audit. _(`ChangeEmployeeManagerCommand`/`Handler` + `POST /api/corehr/employees/{id}/change-manager` (`ChangeEmployeeManagerRequest`, If-Match concurrency) delegate to `IWorkforceMutationService.ChangeManagerAsync`, which close-and-succeeds the current primary relationship and creates the new one in one `SaveChanges`, with self/cycle/cross-tenant/date validation and first-manager handling, returning `EmployeeDetailsDto`. A `ManagerChanged` audit entry is staged in the same transaction. `ChangeEmployeeManagerCommandHandlerTests` (2: first-manager assignment creates the primary relationship; self-manager rejected with `Manager.Self`).)_
- [x] 5.5 Enforce server-side deny-by-default authorization and tenant scoping on terminate, rehire, and change-manager; add authorization tests for permitted, unpermitted, and cross-tenant callers. _(Controllers are deny-by-default: rehire requires `CanManageEmployees`; change-manager requires `CanManageEmployees` or `CanManageReporting`; terminate already gated on `CanManageEmployees`; none declare method role attributes (class-level `[Authorize]` + runtime policy). `EmployeesControllerLifecycleAuthorizationTests` (6: rehire/change-manager permitted send the command, unpermitted return `ForbidResult` with no command sent, and cross-tenant rehire/change-manager throw `EntityNotFoundException` via the fail-closed tenant query filter). Extended `EmployeesControllerAuthorizationTests` reflection theory to cover `Rehire`/`ChangeManager`.)_

## 6. Phase 2 — Employee import

- [x] 6.1 Add `BatchEffectiveDate`, optional row-level `effectiveDate` canonical field, and batch-level `ImportMode` (`BusinessChange`/`Correction`) to the import session/request lifecycle and template/schema. _(Added `EmployeeImportMode` enum + `BatchEffectiveDate`/`ImportMode` to `EmployeeImportSession` (set at `CreatePreviewReady`, date normalized to UTC midnight), EF config (string-converted mode), and migration `20260627001851_AddEmployeeImportBatchSettings` (transient back-fill: `CURRENT_TIMESTAMP` / `BusinessChange`). New optional `effectiveDate` canonical field flows into schema/template/preview (`EmployeeImportPreviewRowDto.EffectiveDate`); rewrote `EnsureTemplateHeaders` into a subset+canonical-order validator that accepts optional columns including `effectiveDate`. `UploadAsync` gained a `(file, batchEffectiveDate, importMode, ct)` overload (old overload delegates with today/`BusinessChange` for back-compat); controller `Upload` reads `batchEffectiveDate`/`importMode` from the form; `EmployeeImportSessionDto` surfaces both. Changing the batch date requires a fresh upload (no in-place mutation endpoint), so prior validation cannot silently persist. Tests: new `UploadAsync_CarriesBatchEffectiveDateAndMode_AndAcceptsEffectiveDateColumn`, updated template-header expectation. Verified: build clean, 513/513 tests, no pending EF model changes.)_
- [x] 6.2 Replace tenant duplicate-employee-number check with match/classification by employee number; classify rows (Create/Unchanged/ProfileCorrection/EmploymentChange/WorkAssignmentChange/ManagerChange/Invalid/Conflicting) and store canonical draft records in normalized rows. _(Validation now matches each row to an existing employee by tenant-scoped `EmployeeNumber` (never email) and removes the `duplicateEmployeeNumberInTenant` block; email collisions with a DIFFERENT existing employee still block. Each valid row is classified against canonical facts (resolved via `IWorkforceCanonicalResolver`) as of its resolved effective date (`row.effectiveDate ?? BatchEffectiveDate`): single headline label by precedence Manager > WorkAssignment > Employment > Profile > Unchanged (Create for unmatched), with the full structured diff (`ProfileChanged/EmploymentChanged/WorkAssignmentChanged/ManagerChanged`) preserved on the canonical draft record. Mode safety: Correction batches reject create rows (`createInCorrectionBatch`) and manager corrections (`managerCorrectionUnsupported`) as blocking. `StoredNormalizedRow` carries classification + change flags + resolved effective date + matched id; preview rows surface `Classification`/`ResolvedEffectiveDate`/`MatchedEmployeeId`/`ChangedFacts` once validated. Header validator relaxed to an ordered canonical subset requiring only identity columns so controlled-update files may omit unchanged columns. Tests: create classification, match-by-number WorkAssignmentChange (no dup-number block), correction-batch-rejects-create. NOTE: canonical publication of matched rows is task 6.4 — `ApplyAsync` still uses the legacy create path and its existing recheck blocks matched-number rows at apply-time until 6.4 lands. Verified: build clean, 516/516 tests.)_
- [x] 6.3 Implement two-level validation (row + batch incl. cross-batch manager cycles, one active employment, one primary assignment, effective-date windows, no cross-tenant refs); compute proposed canonical change-set; enforce mode and correction safety rules. _(Added `effectiveDateBeforeHireDate` (Create rows, batch or row-level date before hire date), `effectiveDateBeforeEmploymentStart` (matched rows, using earliest-employment DB query to distinguish "before start" from "no employment"), and `noActiveEmploymentAtEffectiveDate` (matched rows with WorkAssignment/Employment/Manager changes but no active employment). New issue categories `invalidEffectiveDateWindow` and `importModeViolation`; updated `GetIssueCategory`/`GetIssueFixHint`. Tests: 3 new validation tests. 520/520 tests passing.)_
- [x] 6.4 Rework `ApplyAsync` to all-or-nothing batch publication: re-verify, re-run conflict checks, write the full canonical change-set transactionally, write audit/publication history, roll back fully on failure; remove `SkippedCount` and partial-success semantics; add explicit counts/status. _(Three-phase apply: (1) Stage creates — `Employee.Create` + `StartEmploymentAsync` + `ChangeWorkAssignmentAsync`; (2) Assign managers for creates — `AssignManager` (legacy) + conditional `ChangeManagerAsync` (only when subject has a work assignment); (3) Apply canonical mutations for matched rows — `UpdateEmployeeProfileAsync` / `UpdateEmploymentDetailsAsync` / `ChangeWorkAssignmentAsync` or `CorrectPrimaryWorkAssignmentAsync` / `ChangeManagerAsync`. Pre-flight same-file cycle check (`CheckSameFileManagerCycles`) before any write. `CreatedCount = createRows.Count`, `UnchangedRowCount = unchangedRows.Count`, `PublishedRowCount = createRows + changeRows` on both the history entity and the result DTO; no `SkippedCount` / partial-success term survives (see 6.5).)_
- [x] 6.5 Update preview/history DTOs to show classification, resolved effective date, proposed change-set, errors/warnings, batch mode, and publication status; link canonical records to the import batch. _(Per locked design D8, import history uses explicit non-partial-success terms only: `SourceRowCount` / `ValidatedRowCount` / `CreatedCount` / `UnchangedRowCount` / `PublishedRowCount` / `Status`. The legacy `SkippedCount` and `ValidRowCount` names were removed from the domain entity, EF config, apply-result + history list/detail DTOs, frontend types/panels, and tests; `ValidRowCount`→`ValidatedRowCount`, `SkippedCount`→`UnchangedRowCount`, and a stored `PublishedRowCount` (committed creates + controlled-update changes; unchanged rows never counted) was added to the history entity and surfaced in list/detail responses. Data-preserving forward migration `20260627190940_RenameImportHistoryCountsToCanonicalTerms` renames the columns and backfills `PublishedRowCount = CreatedCount` for pre-existing create-only rows. The only remaining `SkippedCount` in the codebase is the unrelated Identity `WorkforceBulkInviteResponseDto`. Verified: CoreHR build + 422 tests, core type-check + import-panel tests, `has-pending-model-changes` clean.)_
- [x] 6.6 Enforce server-side deny-by-default authorization and tenant scoping on every import stage (upload, validate, preview, publish); add authorization tests including unpermitted publish of an already-validated session and cross-tenant session access. _(Controller is already `[Authorize]` at class level + `CanImportEmployees` runtime check on every endpoint. Added `ApplyAsync_BlocksCrossTenantSessionAccess` test verifying fail-closed tenant query filter throws `EntityNotFoundException` for cross-tenant session IDs.)

## 7. Phase 2 — Workforce contracts and Performance boundary

- [x] 7.1 Refactor `WorkforceContractService` (org-unit members, scope filtering, team/downline/manager-chain/summary) to resolve from `Employment`/`WorkAssignment`/`ManagerRelationship` with `asOfDate`; default today for current screens. _(`IWorkforceCanonicalResolver` injected into `WorkforceContractService`; `ApplyVisibilityScope` → async `ApplyVisibilityScopeAsync` using `ManagerRelationship`; `GetTeamAsync` / `GetDownlineAsync` walk canonical `ManagerRelationship` edges; `GetManagerChainAsync` uses `canonicalResolver.GetManagerChainAsync`; `GetOrgUnitMembersAsync` uses `GetEmployeeIdsInOrgUnitAsync`; `GetEmployeesByScopeAsync` queries `WorkAssignment.OrgUnitId`; `BuildSummariesAsync` resolves direct-report counts, org unit, and manager from canonical facts with legacy fallback for pre-backfill rows; `GetPublishedOrgUnitTreeAsync` member counts from canonical `WorkAssignment`. Build clean, 520/520 tests pass.)_
- [x] 7.2 Refactor `CampaignWorkforceContextService` to read `Employment`/`WorkAssignment`/`ManagerRelationship` as of `AsOf`; rename readiness/delta codes (`MissingPrimaryWorkAssignment`, `ConflictingPrimaryWorkAssignments`, `PrimaryOrgAssignmentChanged`); move internal ids to work-assignment semantics without exposing raw canonical ids. _(`CampaignWorkforceContextService` now treats active canonical `Employment` as the only active-status source, scopes org context to effective primary `WorkAssignment` rows whose parent employment is active, detects conflicting effective primary assignments, resolves primary-manager chains only through effective primary `ManagerRelationship` rows bound to the employee's unique current primary assignment, and emits non-primary relationship candidates with work-assignment ids and `"CoreManagerRelationship"` provenance. Rewrote `CampaignWorkforceContextServiceTests` to seed canonical `Employment`/`WorkAssignment`/`ManagerRelationship` facts, and reconciled stale 7.1 workforce-contract test fixtures to canonical seeding so validation reflects the actual contract surface. Verified with `dotnet build Backend/EY.HRPlatform.CoreHR --no-restore`, focused `dotnet test Backend/EY.HRPlatform.CoreHR.Tests --filter CampaignWorkforceContextServiceTests`, and full `dotnet test Backend/EY.HRPlatform.CoreHR.Tests` (519/519).)_
- [x] 7.3 Update Performance `MarkCycleReadyToLaunch`/`CampaignWorkforceDeltaResolver` to consume the richer canonical context/delta (org and manager changes), keeping the existing contract shape where safe. _(`ICoreWorkforceClient` now supports the signed internal Core campaign-workforce context endpoint; `CampaignResponsibilityWorkItemDto` carries the participant snapshot's org-unit and primary-manager ids; `CampaignWorkforceDeltaResolver` now compares current canonical Core campaign context against preparation-time snapshots to surface subject org changes, subject manager changes, canonical readiness drift, and assignee-primary-manager mismatches, while still preserving the existing `CampaignWorkforceDeltaDto` shape. `MarkCycleReadyToLaunchCommandHandler` now blocks when final approver assignments are no longer valid, not only when an assignee is inactive. Added/updated targeted Performance tests for inactive assignees, primary-manager drift blocking, and non-blocking org-change deltas; reconciled broader readiness/happy-path/cascade fixtures to populate the new Core campaign-context seam. Verified with `dotnet build Backend/EY.HRPlatform.Performance --no-restore`, focused `dotnet test Backend/EY.HRPlatform.Performance.Tests --filter "MarkCycleReadyToLaunchCommandHandlerTests|WorkforceDeltaReconciliationTests|CoreWorkforceClientTests"`, and full `dotnet test Backend/EY.HRPlatform.Performance.Tests` (246/246).)_
- [x] 7.4 Verify Performance snapshots (job title, org, manager) resolve from canonical facts as of date; version/replace an endpoint only where current-vs-as-of meaning cannot be carried safely. _(`PublishCycle` now resolves participant snapshots through a dedicated signed internal Core snapshot seam with explicit `asOf`, while draft preview remains on the current workforce seam. Added `InternalWorkforceSnapshotService` + hidden `internal/corehr/workforce/snapshots/{resolve|by-scope}` endpoints so Performance can request canonical employee/org/job/manager facts as-of without overloading the public current-state contract. `PerformancePopulationResolver` now switches between current and as-of endpoints based on caller intent; publish passes one frozen timestamp through resolution and stores that exact value in `PerformanceCycleParticipant.SnapshotAt`. Added focused Core/Performance tests covering canonical as-of snapshot resolution, resolver endpoint selection, client routing, and publish-time snapshot freezing. Verified with `dotnet build Backend/EY.HRPlatform.CoreHR --no-restore`, `dotnet build Backend/EY.HRPlatform.Performance --no-restore`, focused `dotnet test Backend/EY.HRPlatform.CoreHR.Tests --filter InternalWorkforceSnapshotServiceTests`, focused `dotnet test Backend/EY.HRPlatform.Performance.Tests --filter "PerformancePopulationResolverTests|PublishCycleCommandHandlerTests|CoreWorkforceClientTests"`, full `dotnet test Backend/EY.HRPlatform.CoreHR.Tests` (520/520), and full `dotnet test Backend/EY.HRPlatform.Performance.Tests` (249/249).)_

## 8. Phase 2 — Core frontend Employee Details and import

- [x] 8.1 Build the single Employee Details workspace with Profile / Employment / Work Assignment / Manager / Access sections backed by the composed contract; remove per-entity CRUD screens. _(`EmployeeProfileWorkspace` now renders the five-section composed details workflow (Profile, Employment, Work assignment, Manager, Access) from `EmployeeDetailsDto`, with profile flattening retained only as an internal view-model adapter for legacy sheet components. The tenant employee page now loads `GET /corehr/employees/by-key/{employeeKey}` via `useEmployeeDetails`, and the self-profile page now loads `GET /corehr/employees/{id}` via `useEmployeeDetailsById`, so both entry points are backed by the composed Core contract rather than the legacy profile DTO. Added a dedicated employee-details query key to avoid cache-shape collisions with the old profile hook, and updated focused workspace/hook tests to assert the five-tab details layout and composed endpoint usage. Verified with `pnpm --filter core exec vitest run "src/features/employees/profile/employee-profile-workspace.test.tsx" --reporter=dot`, `pnpm --filter core exec vitest run "src/app/(pages)/employees/use-employees.test.ts" --reporter=dot`, and `pnpm --filter core type-check`.)_
- [x] 8.2 Migrate create/edit dialogs, sheets, roster/profile/org-chart consumers, and `use-employees` to the composed `EmployeeDetails` contract and section-specific mutations.
- [x] 8.3 Replace deactivate/reactivate hooks with `useTerminateEmployee`, `useRehireEmployee`, `useChangeManager`; add direct-report-blocking remediation UX (affected reports + reassignment path).
- [x] 8.4 Update the import workspace: required batch effective date control, import mode selection, optional effective-date column, change-set preview with classifications, and "Published" only on full-batch success.
- [x] 8.5 Compose the Access section as an independent Identity data load with its own loading/error state so Identity failure does not block Core employee details. _(Already correctly isolated: `WorkforceAccountCard` and `EmployeeAccessSummaryBadge` each call `useWorkforceAccountStatus` independently and render error/loading states within their own scope without affecting Core tabs.)_

## 9. Identity boundary

- [x] 9.1 Document `api/corehr/employees/workforce-accounts` as Identity-owned in design/API/Gateway docs; ensure Core does not call Identity to assemble `EmployeeDetails`. _(Design.md D11 documents the boundary. Gateway `corehr-workforce-accounts-route` routes to `identity-cluster` with `owner: identity` metadata. `EmployeeDetailsReadModelService.BuildAsync` confirmed Identity-free — reads only from CoreHR DB. `GetEmployeesQueryHandler` calls `IWorkforceAccountStatusReader` only when filtering by `Access` query param, not to assemble the details payload.)_
- [x] 9.2 Review `ApplicationUser` workforce-duplicating profile fields (e.g. `JobTitle`); remove or stop syncing where unused/duplicative so Identity is not a second workforce source of truth. _(`ApplicationUser.JobTitle`/`Department`/`HireDate` are registration-time snapshots only — no sync exists. `WorkforceBulkProvisioner` sends only `EmployeeId/Email/FirstName/LastName` (no workforce fields), so Core provisioning cannot make Identity a live second source of truth. Fields are benign stale hints for display (e.g., invite emails, admin user listing); callers needing authoritative job-title/org data must read Core's `WorkAssignment`. Removing the columns would be a large Identity-internal change crossing DTOs, migrations, and seeder with no runtime gain — deferred as a dedicated Identity cleanup task outside this change's scope.)_

## 10. Phase 3 — Cleanup and removal

- [x] 10.1 Remove `Position`, `EmployeePositionAssignment`, `EmployeeOrgMembership`, `OrgMembershipType` runtime code (entities, configurations, DbSets, query filters, EF snapshot refs, DTOs, services, obsolete tests). _(Deleted entities `Position`/`EmployeePositionAssignment`/`EmployeeOrgMembership`/`EmployeeReportingRelationship`, enum `OrgMembershipType`, EF configurations for all four, `ReportingRelationshipService` + its `IReportingRelationshipService` registration, backfill code `WorkforceBackfillService`/`OrganizationContextBackfillResolver` + registration, and 6 obsolete test files. Removed legacy DbSets and query filters from `CoreHRDbContext`. Build clean, 498/498 tests pass (22 deleted tests gone).)_
- [x] 10.2 Remove `Employee.ManagerId`/`OrgUnitId`/`JobTitle`/`WorkLocation`/`HireDate`/`Status`/`EmploymentType` properties, `AssignManager`/`AssignOrgUnit`/`Activate`/`Deactivate` (legacy-only), and EF mappings; remove obsolete request/response DTO fields and frontend form fields/types. _(`EmployeeDto` and the legacy CoreHR `EmployeeProfileDto`/`GET .../profile` query path are deleted; the Core frontend no longer exports or queries the flattened `EmployeeProfileDto` shape and its edit sheets now consume `EmployeeDetailsDto` directly. The org-chart reassignment helper also moved off the generic `PUT /employees/{id}` manager update path onto `POST /employees/{id}/change-manager`. Verified with `dotnet build Backend/EY.HRPlatform.CoreHR`, `dotnet test Backend/EY.HRPlatform.CoreHR.Tests --no-build` (491/491), `pnpm --dir Frontend --filter core exec vitest run \"src/app/(pages)/employees/use-employees.test.ts\" --reporter=dot` (20/20), and `pnpm --dir Frontend --filter core type-check`.)_
- [x] 10.3 Remove legacy `DELETE {id}` and `reactivate` routes/commands/handlers and `Employee.Activate()`/`Deactivate()` if only legacy status flips; delete temporary migration-only code, transitional readers, and projection writers. _(Deleted `DeactivateEmployeeCommand`/`Handler` + `ReactivateEmployeeCommand`/`Handler`, removed both controller actions + usings, deleted `EmployeeHierarchyService` (only caller was the deleted deactivate handler), removed the DI registration, and removed `IEmployeeHierarchyService` from `EmployeeImportWorkflowService` constructor + field. Removed `Employee.Activate()` (no remaining callers). `Employee.Deactivate()` kept temporarily (still called by `CoreHRSeeder` for demo data; will drop with seeder update in 10.2). `AssignOrgUnit`/`AssignManager` calls in import workflow retained as transitional bridges — `EmployeeReadModelPolicy`/`EmployeeReadinessPolicy` still read legacy fields; both will be removed when those policies are replaced in 10.2. Updated 4 import tests to assert canonical `WorkAssignment`/`ManagerRelationship` instead of legacy entity fields. 493/493 tests pass.)_
- [x] 10.4 Add the forward cleanup migration that drops legacy employee workforce columns and removed-entity tables. _(Added EF migration `20260627152517_RemoveLegacyEmployeeWorkforceRuntime`, which drops `Employees.ManagerId`/`OrgUnitId`/`JobTitle`/`WorkLocation`/`HireDate`/`Status`/`EmploymentType`, removes the `IX_Employees_ManagerId`/`IX_Employees_OrgUnitId` indexes and FKs, and drops the retired `Positions`/`EmployeePositionAssignments`/`EmployeeOrgMemberships`/`EmployeeReportingRelationships` tables. `dotnet build Backend/EY.HRPlatform.CoreHR` passes after the runtime cleanup.)_
- [x] 10.5 Run a repository search for retired identifiers (`ManagerId`, `AssignManager`, `OrgUnitId`, `AssignOrgUnit`, `JobTitle`, `WorkLocation`, `Employee.HireDate`, `Employee.Status`, `Employee.EmploymentType`, `EmployeeOrgMembership`, `OrgMembershipType`, `EmployeePositionAssignment`, `Position`, `LegacyPositionTitle`, `SubjectPositionAssignmentId`, `ManagerPositionAssignmentId`) and confirm only historical/cleanup migrations reference them. _(Ran an exact retired-surface sweep after the Phase 3 cleanup using `rg`-backed repository search for `AssignManager`, `AssignOrgUnit`, `Employee.HireDate`, `Employee.Status`, `Employee.EmploymentType`, `EmployeeOrgMembership`, `OrgMembershipType`, `EmployeePositionAssignment`, `LegacyPositionTitle`, `SubjectPositionAssignmentId`, `ManagerPositionAssignmentId`, `Employee.ManagerId`, `Employee.OrgUnitId`, `Employee.JobTitle`, and `Employee.WorkLocation`. Result: no runtime or test references remain; matches are limited to OpenSpec/docs plus historical/final-cleanup migrations. Broad homonyms like canonical `WorkAssignment.OrgUnitId`/`JobTitle` still exist by design, so the verification used exact retired member/type names rather than raw token presence.)_

## 11. Cross-boundary verification

- [x] 11.1 Update/replace obsolete tests and add canonical-model, lifecycle, import create/update/correction, audit-entry, responsible-manager, and tenant-isolation tests for all new entities/endpoints. _(Replaced stale legacy employee/read tests with canonical coverage (`EmployeeIdentityTests`, `GetEmployeesCanonicalQueryHandlerTests`, rewritten `GetEmployeeReportingLinesQueryHandlerTests`), removed obsolete controller/profile expectations, and updated remaining CoreHR tests to seed/assert canonical `Employment`/`WorkAssignment`/`ManagerRelationship` facts instead of removed runtime members (`AssignOrgUnit`, `AssignManager`, `Employee.HireDate`, legacy deactivate/reactivate semantics). Repaired `EmployeeImportWorkflowTests` canonical seeding so import create/update/correction scenarios validate against tenant-visible org context. Verified with `dotnet test Backend/EY.HRPlatform.CoreHR.Tests/EY.HRPlatform.CoreHR.Tests.csproj` (422/422 passing).)_
- [x] 11.2 Verify fresh-database and upgrade-from-current-schema migrations apply with no pending EF model changes and backfill validation passes (`dotnet build`, ef list/update, has-pending-model-changes). _(Verified `dotnet build Backend/EY.HRPlatform.CoreHR/EY.HRPlatform.CoreHR.csproj`, `dotnet ef migrations list`, and `dotnet ef migrations has-pending-model-changes` (`No changes have been made to the model since the last migration.`). Then exercised live Postgres migration paths with disposable databases via `dotnet ef database update --connection ...`: fresh database `fusion_corehr_verify_fresh2` migrated cleanly from `InitialCoreHR` through `20260627152517_RemoveLegacyEmployeeWorkforceRuntime`, and upgrade database `fusion_corehr_verify_upgrade2` first migrated to `20260622231100_AddOrgUnitResponsibleManager` and then upgraded cleanly through the three workforce-hardening migrations. No migration/backfill validation failure surfaced during either path; disposable verification databases were dropped after the run.)_
- [x] 11.3 Run Core frontend checks for Employee Details and import flows and Identity-independent Access loading (`pnpm --filter core test`, `pnpm lint`, `pnpm type-check`). _(Verified `pnpm --filter core test`, `pnpm lint`, and `pnpm type-check` from `Frontend/`; all exited `0`. `pnpm lint` still reports pre-existing warnings in other apps and core screens, but it completes successfully and does not block the verification gate.)_
- [x] 11.4 Run Performance contract tests proving workforce facts resolve as of date from canonical Core facts without exposing raw canonical ids, and run targeted backend test suites. _(Verified focused CoreHR tests (`InternalWorkforceSnapshotServiceTests|CampaignWorkforceContextServiceTests`, plus the broader interval/lifecycle/auth subset) and focused Performance tests (`PerformancePopulationResolverTests|PublishCycleCommandHandlerTests|CoreWorkforceClientTests|WorkforceDeltaReconciliationTests|MarkCycleReadyToLaunchCommandHandlerTests`). All targeted runs passed.)_
- [x] 11.5 Exercise end-to-end scenarios: create employee, update details, import creates+updates, change manager, work-assignment change, termination with direct-report blocking (manager-side not auto-closed), rehire, Performance population/manager resolution as of date, and independent Access load. _(Exercised the scenario set through focused suites spanning `CreateEmployeeComposedCommandHandlerTests`, `UpdateEmployeeComposedCommandHandlerTests`, `EmployeeImportWorkflowTests`, `ChangeEmployeeManagerCommandHandlerTests`, `WorkforceMutationServiceTests`, `TerminateEmployeeCommandHandlerTests`, `RehireEmployeeCommandHandlerTests`, `PerformancePopulationResolverTests`, `PublishCycleCommandHandlerTests`, `CoreWorkforceClientTests`, and the core access-loading tests (`use-workforce-accounts` / `employee-access-management-sheet`). All targeted runs passed.)_
- [x] 11.6 Verify the `[EffectiveFrom, EffectiveTo)` convention end-to-end (as-of boundary, adjacency, close-and-succeed) across employment/assignment/manager; verify org-context backfill precedence and conflict fail-fast; verify deny-by-default authorization tests pass for lifecycle, import stages, responsible-manager, and audit read surface.
