## 1. Phase 1 — Canonical model and persistence

- [x] 1.1 Add `Employment` entity (employee, start/`EffectiveFrom`, optional end/`EffectiveTo`, status, employment type, source/provenance, audit) with `IEntityTypeConfiguration`, tenant-scoped indexes, and a one-active-employment-per-employee constraint strategy.
- [x] 1.2 Add `WorkAssignment` entity (employment ref, org unit, plain-text job title, optional work location, primary flag, effective from/to, source/provenance) with configuration, tenant indexes, and primary/overlap constraints.
- [x] 1.7 Implement and centralize the half-open `[EffectiveFrom, EffectiveTo)` interval convention (active-as-of predicate, close-and-succeed helper, overlap/contiguity checks) shared by `Employment`, `WorkAssignment`, and `ManagerRelationship`; add unit tests for boundary, adjacency, and null-end cases.
- [x] 1.3 Refactor `EmployeeReportingRelationship` into `ManagerRelationship` referencing `SubjectWorkAssignmentId`/`ManagerWorkAssignmentId`; update configuration FKs to `WorkAssignment`; keep effective dating and type. _(Added the refactored `ManagerRelationship` (WorkAssignment FKs) alongside the legacy `EmployeeReportingRelationship`, which still has Phase 2 consumers; the legacy entity is removed in the Phase 3 cleanup (task 10.1) to keep the build green at this phase boundary.)_
- [x] 1.4 Add append-only tenant-scoped `WorkforceAuditEntry` entity and configuration (tenant, entity type/id, action, actor, timestamp, effective date, source/import reference, change details).
- [x] 1.5 Register new DbSets and fail-closed tenant query filters in `CoreHRDbContext`; confirm `ApplyConfigurationsFromAssembly` picks up the new configurations.
- [x] 1.6 Create the EF migration that adds the canonical tables (canonical model added alongside legacy columns, which remain temporarily as backfill sources only). _(Migration `20260626195348_AddWorkforceCanonicalModel` — additive; creates Employments/WorkAssignments/ManagerRelationships/WorkforceAuditEntries with filtered-unique active/primary constraints.)_

## 2. Phase 1 — Backfill and fail-fast validation

- [x] 2.1 Implement deterministic, tenant-aware backfill: `Employment` from `HireDate`→`EffectiveFrom`/`Status`/`EmploymentType`, then primary `WorkAssignment` from org-context precedence/`JobTitle`/`WorkLocation`, then `ManagerRelationship` from `ManagerId`, all with `Source = Migration` provenance and `[from, to)` dating. _(`Infrastructure/Backfill/WorkforceBackfillService` — per-tenant, idempotent; inactive employees end at `UpdatedAt`.)_
- [x] 2.2 Implement the deterministic organization-context precedence (1: existing canonical assignment data, 2: primary/home `EmployeeOrgMembership`, 3: legacy `Employee.OrgUnitId`) and fail fast on disagreement or multiple active primary memberships per employee/date. _(`OrganizationContextBackfillResolver`.)_
- [x] 2.3 Implement fail-fast backfill validation (invalid/missing references, missing employee numbers for matching, missing/inactive org units, cross-tenant links, overlapping `[from, to)` facts, self-management, manager cycles, conflicting org-context sources, multiple active primary memberships, invalid responsible-manager refs) that aborts with actionable diagnostics — no silent defaults, no quarantine-and-continue. _(Integrated into the backfill service; aborts via `WorkforceBackfillException` carrying all diagnostics. Employee-number matching is an import-side concern (Section 6), not backfill.)_
- [x] 2.4 Add migration/backfill tests over representative data covering both success and each fail-fast condition. _(`WorkforceBackfillServiceTests` — 13 tests: success, idempotency, inactive end-dating, no-org gap, + each fail-fast code.)_

## 3. Phase 2 — Canonical resolvers and domain services

- [x] 3.1 Implement canonical resolver services (current employment, primary work assignment org/job/location, primary manager and manager chain, org-scope membership from `WorkAssignment.OrgUnitId`) with explicit `asOfDate` support. _(`Features/Workforce/Services/WorkforceCanonicalResolver` — `IWorkforceCanonicalResolver`, half-open as-of predicate, cycle-guarded chain. Consumers (readiness/authorization/contracts) are repointed in tasks 3.2–7.x.)_
- [x] 3.2 Implement explicit domain commands/services: `UpdateEmployeeProfile`, employment start/end, `ChangeWorkAssignment`, `ChangeManager`, with effective-dated history and per-action `WorkforceAuditEntry` writes in the same transaction. _(`Features/Workforce/Services/WorkforceMutationService` (`IWorkforceMutationService`) — unit-of-work staging (no internal `SaveChanges`, so the orchestrating handler commits fact + audit atomically in one transaction); close-and-succeed `[from, to)` transitions; canonical invariants (single active employment, single active primary assignment, org-unit tenant/active validity, manager self/cross-tenant/cycle); `Result<T>` with `Error` codes; Local-aware lookups so freshly-staged facts in a composed create resolve. Added focused `Employee.UpdateProfile` (identity-only). `WorkforceMutationServiceTests` — 17 tests across all four operations incl. audit writes, close-and-succeed, and each fail-fast branch. Consumed by endpoints/lifecycle/import in tasks 4.x–6.x.)_
- [x] 3.3 Add `OrgUnit.ResponsibleManagerEmployeeId` validation (same-tenant, active employment) to create/update org-unit handlers and audit changes; expose resolved responsible-manager data where UI/Performance needs it; enforce server-side deny-by-default authorization and tenant scoping on responsible-manager changes with authorization tests. _(`Features/OrgUnits/Services/ResponsibleManagerService` (`IResponsibleManagerService`) — validates same-tenant (fail-closed filter) employee with an active canonical `Employment` (via resolver, never legacy status); stages a `ResponsibleManagerChanged` audit only when the value changes, in the handler's transaction. Wired into Create/Update org-unit handlers; id already exposed on `OrgUnitDto`. Authz: controller is already deny-by-default (`[Authorize(Roles=HRAdmin)]` + `CanManageStructure`→Forbid); added method-level role assertions. `ResponsibleManagerServiceTests` (7: null/active/unknown/no-employment/cross-tenant/audit-changed/audit-unchanged) + controller authz Theory.)_
- [x] 3.4 If `WorkforceAuditEntry` is exposed through any read API, enforce server-side deny-by-default authorization and tenant scoping on that surface, with authorization and tenant-isolation tests. _(No read API for `WorkforceAuditEntry` exists in this milestone (writes only) — building an authorized read endpoint now would be dead code (D3/non-goals), so the conditional authz requirement is deferred to whenever a surface is introduced. The entity is already fail-closed tenant-scoped in `CoreHRDbContext`; added `WorkforceAuditTenantIsolationTests` (2) pinning that any future read returns only the current tenant's entries and nothing under an unresolved tenant.)_

## 4. Phase 2 — Employee endpoints and Employee Details

- [x] 4.1 Refactor `POST api/corehr/employees` to a composed create that orchestrates `Employee`, `Employment`, primary `WorkAssignment`, and optional `ManagerRelationship` atomically and returns `EmployeeDetails`. _(`CreateEmployeeCommandHandler` now stages `Employee` + `StartEmployment` + `ChangeWorkAssignment` + optional `ChangeManager` through `IWorkforceMutationService` and commits once; `POST /api/corehr/employees` returns the new Core-only `EmployeeDetailsDto` built by `EmployeeDetailsReadModelService` (canonical employment/assignment/manager, history summary, readiness, no Identity fields). Added `CreateEmployeeComposedCommandHandlerTests`; updated legacy handler tests to canonical semantics. Verified with `dotnet build Backend/EY.HRPlatform.CoreHR` and full `Backend/EY.HRPlatform.CoreHR.Tests` (495/495).)_
- [x] 4.2 Refactor `PUT api/corehr/employees/{id}` into a composed, non-generic update delegating to explicit domain services; reject lifecycle operations that require dedicated actions. _(`UpdateEmployeeCommandHandler` now returns `EmployeeDetailsDto`, uses `IWorkforceMutationService` for profile, active-employment, and active-primary-assignment corrections, and rejects generic `manager`/`hireDate` updates with dedicated-action validation errors. Added correction methods to `WorkforceMutationService`, `WorkAssignment.UpdateDetails`, `Employee.UpdateEmployeeNumber`, and `WorkforceAuditAction.EmploymentUpdated`; rewired `PUT /api/corehr/employees/{id}` to return the composed details response and map mutation failures. Added `UpdateEmployeeComposedCommandHandlerTests` and replaced legacy update-handler coverage with canonical expectations. Verified with `dotnet build Backend/EY.HRPlatform.CoreHR` and full `Backend/EY.HRPlatform.CoreHR.Tests` (489/489).)_
- [x] 4.3 Build the composed `EmployeeDetails` read model (Core-owned facts only) and update profile/list/by-key read endpoints to source employment/org/job/manager from canonical resolvers; move direct-report authorization scope to `ManagerRelationship`. _(`GetEmployeeById`/`GetEmployeeByKey` now return `EmployeeDetailsDto` via the canonical `EmployeeDetailsReadModelService`; profile/list/reporting-line queries now resolve employment/org/job/manager/direct reports from canonical facts first, with a transitional legacy-only fallback only when a fixture or pre-backfill row has no canonical facts yet. Direct-report scope now follows the returned manager fact sourced from `ManagerRelationship`, and reporting lines traverse canonical relationships with the same staged fallback. Added canonical-divergence coverage in employee read-handler tests. Verified with `dotnet build Backend/EY.HRPlatform.CoreHR` and full `Backend/EY.HRPlatform.CoreHR.Tests` (492/492).)_
- [x] 4.4 Re-implement tenant-publish org-unit retirement safety checks against active primary `WorkAssignment.OrgUnitId` instead of `Employee.OrgUnitId`. _(`PublishTenantStructureCommandHandler` now blocks retirement using active primary canonical `WorkAssignment` rows joined to active canonical `Employment` rows, with a narrow staged fallback to legacy `Employee.OrgUnitId` only for active employees that do not yet have an active primary assignment. Added publish-block regression coverage for canonical-only org assignment in `TenantSetupPublishingCommandHandlerTests`. Verified with `dotnet build Backend/EY.HRPlatform.CoreHR` and full `Backend/EY.HRPlatform.CoreHR.Tests` (493/493).)_

## 5. Phase 2 — Lifecycle actions

- [x] 5.1 Implement `POST {id}/terminate` (end active employment, primary assignment, and **subject-side** manager relationships at effective date; preserve employee/history) with active-employment validation and audit; do not auto-close manager-side relationships. _(`TerminateEmployeeCommandHandler` + `POST /api/corehr/employees/{id}/terminate` wired through new `IWorkforceMutationService.TerminateEmployeeAsync`; terminates canonical employment + primary assignment + subject-side manager links in one `SaveChanges`, preserves manager-side links for later 5.2 blocking, and writes transactional termination audit with optional note. Added service/handler/auth-route coverage; verified with targeted lifecycle tests, `dotnet build Backend/EY.HRPlatform.CoreHR --no-restore`, and full `Backend/EY.HRPlatform.CoreHR.Tests`.)_
- [x] 5.2 Implement termination direct-report blocking using active primary `ManagerRelationship` (incoming/manager-side links must already be ended via reassignment effective ≤ termination date), returning affected direct reports; no auto/skip-level/fallback reassignment. _(`WorkforceMutationService.TerminateEmployeeAsync` now blocks on active incoming primary `ManagerRelationship` rows pointing to the terminating assignment as of the termination date, using unit-of-work-aware local+persistent resolution so same-transaction reassignments effective ≤ termination date are honored. Blocking failures return `Employment.TerminationBlockedByDirectReports` with the affected direct reports identified in the validation message; no manager-side links are auto-closed or reassigned. Added mutation-service coverage for blocked termination, successful termination after same-date reassignment, and preserved pre-ended manager-side links. Verified with `dotnet build Backend/EY.HRPlatform.CoreHR --no-restore`, targeted lifecycle tests, and full `Backend/EY.HRPlatform.CoreHR.Tests`.)_
- [x] 5.3 Implement `POST {id}/rehire` (new employment + primary assignment + optional manager; never reopen prior employment) with validation and audit. _(`RehireEmployeeCommand`/`Handler` + `POST /api/corehr/employees/{id}/rehire` (`RehireEmployeeRequest`, If-Match concurrency) compose `StartEmployment` + `ChangeWorkAssignment` + optional `ChangeManager` through `IWorkforceMutationService` and commit once, returning `EmployeeDetailsDto`. `StartEmploymentAsync` rejects a second active employment, enforcing the no-active-employment precondition without reopening the prior (ended) employment; per-fact `WorkforceAuditEntry` rows are staged by the mutation service. `RehireEmployeeCommandHandlerTests` (2: rehire-after-termination creates new employment+assignment with prior ended unchanged; active-employment blocks with `Employment.AlreadyActive`).)_
- [x] 5.4 Implement `POST {id}/change-manager` (atomic end-current/create-new primary manager relationship using `[from, to)` close-and-succeed; self/cycle/cross-tenant/date validation; first-manager case) with audit. _(`ChangeEmployeeManagerCommand`/`Handler` + `POST /api/corehr/employees/{id}/change-manager` (`ChangeEmployeeManagerRequest`, If-Match concurrency) delegate to `IWorkforceMutationService.ChangeManagerAsync`, which close-and-succeeds the current primary relationship and creates the new one in one `SaveChanges`, with self/cycle/cross-tenant/date validation and first-manager handling, returning `EmployeeDetailsDto`. A `ManagerChanged` audit entry is staged in the same transaction. `ChangeEmployeeManagerCommandHandlerTests` (2: first-manager assignment creates the primary relationship; self-manager rejected with `Manager.Self`).)_
- [x] 5.5 Enforce server-side deny-by-default authorization and tenant scoping on terminate, rehire, and change-manager; add authorization tests for permitted, unpermitted, and cross-tenant callers. _(Controllers are deny-by-default: rehire requires `CanManageEmployees`; change-manager requires `CanManageEmployees` or `CanManageReporting`; terminate already gated on `CanManageEmployees`; none declare method role attributes (class-level `[Authorize]` + runtime policy). `EmployeesControllerLifecycleAuthorizationTests` (6: rehire/change-manager permitted send the command, unpermitted return `ForbidResult` with no command sent, and cross-tenant rehire/change-manager throw `EntityNotFoundException` via the fail-closed tenant query filter). Extended `EmployeesControllerAuthorizationTests` reflection theory to cover `Rehire`/`ChangeManager`.)_

## 6. Phase 2 — Employee import

- [x] 6.1 Add `BatchEffectiveDate`, optional row-level `effectiveDate` canonical field, and batch-level `ImportMode` (`BusinessChange`/`Correction`) to the import session/request lifecycle and template/schema. _(Added `EmployeeImportMode` enum + `BatchEffectiveDate`/`ImportMode` to `EmployeeImportSession` (set at `CreatePreviewReady`, date normalized to UTC midnight), EF config (string-converted mode), and migration `20260627001851_AddEmployeeImportBatchSettings` (transient back-fill: `CURRENT_TIMESTAMP` / `BusinessChange`). New optional `effectiveDate` canonical field flows into schema/template/preview (`EmployeeImportPreviewRowDto.EffectiveDate`); rewrote `EnsureTemplateHeaders` into a subset+canonical-order validator that accepts optional columns including `effectiveDate`. `UploadAsync` gained a `(file, batchEffectiveDate, importMode, ct)` overload (old overload delegates with today/`BusinessChange` for back-compat); controller `Upload` reads `batchEffectiveDate`/`importMode` from the form; `EmployeeImportSessionDto` surfaces both. Changing the batch date requires a fresh upload (no in-place mutation endpoint), so prior validation cannot silently persist. Tests: new `UploadAsync_CarriesBatchEffectiveDateAndMode_AndAcceptsEffectiveDateColumn`, updated template-header expectation. Verified: build clean, 513/513 tests, no pending EF model changes.)_
- [ ] 6.2 Replace tenant duplicate-employee-number check with match/classification by employee number; classify rows (Create/Unchanged/ProfileCorrection/EmploymentChange/WorkAssignmentChange/ManagerChange/Invalid/Conflicting) and store canonical draft records in normalized rows.
- [ ] 6.3 Implement two-level validation (row + batch incl. cross-batch manager cycles, one active employment, one primary assignment, effective-date windows, no cross-tenant refs); compute proposed canonical change-set; enforce mode and correction safety rules.
- [ ] 6.4 Rework `ApplyAsync` to all-or-nothing batch publication: re-verify, re-run conflict checks, write the full canonical change-set transactionally, write audit/publication history, roll back fully on failure; remove `SkippedCount` and partial-success semantics; add explicit counts/status.
- [ ] 6.5 Update preview/history DTOs to show classification, resolved effective date, proposed change-set, errors/warnings, batch mode, and publication status; link canonical records to the import batch.
- [ ] 6.6 Enforce server-side deny-by-default authorization and tenant scoping on every import stage (upload, validate, preview, publish); add authorization tests including unpermitted publish of an already-validated session and cross-tenant session access.

## 7. Phase 2 — Workforce contracts and Performance boundary

- [ ] 7.1 Refactor `WorkforceContractService` (org-unit members, scope filtering, team/downline/manager-chain/summary) to resolve from `Employment`/`WorkAssignment`/`ManagerRelationship` with `asOfDate`; default today for current screens.
- [ ] 7.2 Refactor `CampaignWorkforceContextService` to read `Employment`/`WorkAssignment`/`ManagerRelationship` as of `AsOf`; rename readiness/delta codes (`MissingPrimaryWorkAssignment`, `ConflictingPrimaryWorkAssignments`, `PrimaryOrgAssignmentChanged`); move internal ids to work-assignment semantics without exposing raw canonical ids.
- [ ] 7.3 Update Performance `MarkCycleReadyToLaunch`/`CampaignWorkforceDeltaResolver` to consume the richer canonical context/delta (org and manager changes), keeping the existing contract shape where safe.
- [ ] 7.4 Verify Performance snapshots (job title, org, manager) resolve from canonical facts as of date; version/replace an endpoint only where current-vs-as-of meaning cannot be carried safely.

## 8. Phase 2 — Core frontend Employee Details and import

- [ ] 8.1 Build the single Employee Details workspace with Profile / Employment / Work Assignment / Manager / Access sections backed by the composed contract; remove per-entity CRUD screens.
- [ ] 8.2 Migrate create/edit dialogs, sheets, roster/profile/org-chart consumers, and `use-employees` to the composed `EmployeeDetails` contract and section-specific mutations.
- [ ] 8.3 Replace deactivate/reactivate hooks with `useTerminateEmployee`, `useRehireEmployee`, `useChangeManager`; add direct-report-blocking remediation UX (affected reports + reassignment path).
- [ ] 8.4 Update the import workspace: required batch effective date control, import mode selection, optional effective-date column, change-set preview with classifications, and "Published" only on full-batch success.
- [ ] 8.5 Compose the Access section as an independent Identity data load with its own loading/error state so Identity failure does not block Core employee details.

## 9. Identity boundary

- [ ] 9.1 Document `api/corehr/employees/workforce-accounts` as Identity-owned in design/API/Gateway docs; ensure Core does not call Identity to assemble `EmployeeDetails`.
- [ ] 9.2 Review `ApplicationUser` workforce-duplicating profile fields (e.g. `JobTitle`); remove or stop syncing where unused/duplicative so Identity is not a second workforce source of truth.

## 10. Phase 3 — Cleanup and removal

- [ ] 10.1 Remove `Position`, `EmployeePositionAssignment`, `EmployeeOrgMembership`, `OrgMembershipType` runtime code (entities, configurations, DbSets, query filters, EF snapshot refs, DTOs, services, obsolete tests).
- [ ] 10.2 Remove `Employee.ManagerId`/`OrgUnitId`/`JobTitle`/`WorkLocation`/`HireDate`/`Status`/`EmploymentType` properties, `AssignManager`/`AssignOrgUnit`/`Activate`/`Deactivate` (legacy-only), and EF mappings; remove obsolete request/response DTO fields and frontend form fields/types.
- [ ] 10.3 Remove legacy `DELETE {id}` and `reactivate` routes/commands/handlers and `Employee.Activate()`/`Deactivate()` if only legacy status flips; delete temporary migration-only code, transitional readers, and projection writers.
- [ ] 10.4 Add the forward cleanup migration that drops legacy employee workforce columns and removed-entity tables.
- [ ] 10.5 Run a repository search for retired identifiers (`ManagerId`, `AssignManager`, `OrgUnitId`, `AssignOrgUnit`, `JobTitle`, `WorkLocation`, `Employee.HireDate`, `Employee.Status`, `Employee.EmploymentType`, `EmployeeOrgMembership`, `OrgMembershipType`, `EmployeePositionAssignment`, `Position`, `LegacyPositionTitle`, `SubjectPositionAssignmentId`, `ManagerPositionAssignmentId`) and confirm only historical/cleanup migrations reference them.

## 11. Cross-boundary verification

- [ ] 11.1 Update/replace obsolete tests and add canonical-model, lifecycle, import create/update/correction, audit-entry, responsible-manager, and tenant-isolation tests for all new entities/endpoints.
- [ ] 11.2 Verify fresh-database and upgrade-from-current-schema migrations apply with no pending EF model changes and backfill validation passes (`dotnet build`, ef list/update, has-pending-model-changes).
- [ ] 11.3 Run Core frontend checks for Employee Details and import flows and Identity-independent Access loading (`pnpm --filter core test`, `pnpm lint`, `pnpm type-check`).
- [ ] 11.4 Run Performance contract tests proving workforce facts resolve as of date from canonical Core facts without exposing raw canonical ids, and run targeted backend test suites.
- [ ] 11.5 Exercise end-to-end scenarios: create employee, update details, import creates+updates, change manager, work-assignment change, termination with direct-report blocking (manager-side not auto-closed), rehire, Performance population/manager resolution as of date, and independent Access load.
- [ ] 11.6 Verify the `[EffectiveFrom, EffectiveTo)` convention end-to-end (as-of boundary, adjacency, close-and-succeed) across employment/assignment/manager; verify org-context backfill precedence and conflict fail-fast; verify deny-by-default authorization tests pass for lifecycle, import stages, responsible-manager, and audit read surface.
