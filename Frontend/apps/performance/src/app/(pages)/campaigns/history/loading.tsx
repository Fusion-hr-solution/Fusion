import { CampaignHistoryLoading } from "@/components/campaigns/campaign-history-page";

// Segment loading boundary for the closed-campaign record.
export default function CampaignHistoryRouteLoading() {
  return <CampaignHistoryLoading />;
}
