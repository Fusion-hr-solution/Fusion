module.exports = {
  root: true,
  extends: ["@repo/eslint-config/base"],
  parserOptions: {
    project: true,
  },
  // Vendored shadcn primitives are generated by the shadcn CLI / preset and
  // are intentionally excluded from lint (theme/tokens governed by the preset).
  ignorePatterns: ["src/components/ui/**", "dist"],
};
